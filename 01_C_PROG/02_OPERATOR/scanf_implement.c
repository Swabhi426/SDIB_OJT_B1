#include<stdio.h>
int main()
{
    int radius;
    float area;
    printf("enter the radius of the circle");
    scanf("%d",&radius);
    area=3.14*radius*radius;
    printf("AREA:%f",area);
}